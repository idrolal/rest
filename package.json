{
  "name": "rest",
  "version": "1.0.0",
  "description": "При подтягивании изменений пакетов делаем NPM CI! При установке пакета убедитесь, что пакет раннее не был установлен ( в package.json)   Если его нет - вводим команду npm i / npm -D i",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npx sequelize db:migrate && npx sequelize db:seed:all && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/idrolal/rest.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/idrolal/rest/issues"
  },
  "homepage": "https://github.com/idrolal/rest#readme",
  "dependencies": {
    "sequelize-cli": "^6.4.1"
  }
}
